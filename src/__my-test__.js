module.exports = {
    development: {
        // default configs here
    },
    staging: {
        // overwrite config props in development, 
        // you don't need to redeclare all config properties, only the overwrites here 
    },
    production: {
        // overwrite config props in development, 
        // you don't need to redeclare all config properties, only the overwrites here
    }
}